type Query {
    getAllProducts: [Product!]!
    getProduct(id: ID!): Product
    getProductByCategory(category: String): [Product]
}

type Product {
    id: ID!
    name: String!
    description: String
    category: String
    price: String
    image: String
    quantity: String
}